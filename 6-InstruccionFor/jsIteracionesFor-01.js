function mostrar()
{

	for (let i = 1; i <11 ; i ++){
		console.log (i);
	}
	/* let contador (solo se puede usar dentro del for) ; contador <5 ; contador ++
	o la declaro fuera y la uso dentro
	for (variable de control y la declaro ; condicion para que deje de ejecutarse ; modificacion de la variable){
		console.log (contador) Muestra todos los numeros del contador juntos
	}
	Se usa cuando sabemos cuantas veces se tiene que ejecutar la iteraccion y como
	se modifica

	"contador" se suele llamar "i"

	Quedaria:

	for (let i = 0; i < 5; i ++){
		console.log (i);
	}

	las 3 partes de dentro no son obligatorias, y si no pongo la condicion es un bucle infinito
	De todas maneras, se respetan las dos ;;
	*/
}